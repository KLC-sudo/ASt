{
  "$schema": "https://schema.railpack.com",
  "build": {
    "commands": {
      "build": "cd api && npx prisma generate && npm run build"
    }
  },
  "deploy": {
    "startCommand": "cd api && node .next/standalone/server.js",
    "healthcheckPath": "/"
  }
}
